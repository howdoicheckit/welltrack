# ─── Frontend (GitHub Pages build) ───
# Set these as GitHub repo secrets (Settings → Secrets → Actions)
VITE_API_URL=https://your-droplet-domain.com
VITE_API_KEY=your-secret-api-key

# ─── Backend (Droplet) ───
# Set these in your Droplet environment or docker run -e flags
API_KEY=your-secret-api-key          # Must match VITE_API_KEY above
ALLOWED_ORIGIN=https://howdoicheckit.github.io
PORT=3001
